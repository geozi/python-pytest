def sort(word):
    
    """ Sorts a string.

    Args:
        word (str): A string provided by the caller.

    Returns:
        str: A sorted version of the provided string.
    """
    
    return ''.join(sorted(list(word)))